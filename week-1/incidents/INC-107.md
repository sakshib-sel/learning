# INC-107 -- Regional Payment Degradation

Checkout failures are elevated in `eu-west-1`.

`in-south-1` and `us-east-1` appear healthy.

No deployment occurred during the incident window.

One dashboard reports a 6% global error rate, while a regional dashboard reports
18% in `eu-west-1`.

The incident commander asks for the next two observations that would most reduce
uncertainty.
